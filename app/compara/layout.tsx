import type { Metadata } from "next";
export const metadata: Metadata = { title: "Compară profilele", description: "Compară 2-3 profile educaționale side-by-side." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
