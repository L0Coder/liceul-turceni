import type { Metadata } from "next";
export const metadata: Metadata = { title: "Compară profilurile", description: "Compară 2-3 profiluri educaționale comparativ." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
